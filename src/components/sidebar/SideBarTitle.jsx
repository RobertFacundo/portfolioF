import React from "react";
import {StyledSideBarTitle, Wrapper} from '../../styles/SideBarTitle.js';

const SideBarTitle = ()=>{
    return (
       <Wrapper>
         <StyledSideBarTitle/>
       </Wrapper>
    )
};

export default SideBarTitle;